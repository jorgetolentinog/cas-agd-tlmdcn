openapi: 3.0.0
servers:
  - url: http://localhost:3000/dev
  - url: https://q067qpiah5.execute-api.us-east-1.amazonaws.com/dev
info:
  version: "0.0.0"
  title: cas-agd-tlmdcn
tags:
  - name: availability
    description: Módulo de disponibilidad
paths:
  /availability/professional/{professionalId}:
    get:
      tags:
        - availability
      parameters:
        - in: path
          name: professionalId
          schema:
            type: string
            example: 2048
          required: true
        - in: query
          name: startDate
          schema:
            type: string
            example: "2022-11-01"
          required: true
        - in: query
          name: endDate
          schema:
            type: string
            example: "2022-12-01"
          required: true
      responses:
        200:
          description: ok
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvailabilityGetResponse"
components:
  schemas:
    AvailabilityGetResponse:
      type: array
      example: []
